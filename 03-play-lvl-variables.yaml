- name: play level vars
  hosts: local
  connection: local
  vars:
    course: aws devops
    duration: 120hrs
    trainer: shiva
  tasks:
    -name: print course details
     builtin.ansible.debug
     msg: "course details are {{ course }} {{ duration }} {{ trainer }}"
